# 项目规则

## 主控平台

- 本项目以 RoboMaster C 板（STM32F407IGH6）为主控。
- 外设选型、引脚分配、DMA、定时器、中断、通信接口及下载调试配置，必须以 C 板原理图和硬件资源为准，不得按照通用 STM32F407 开发板进行假设。

## 代码框架结构

### 总体架构

- 项目采用 `STM32CubeMX 生成底座 + STM32 HAL + FreeRTOS/CMSIS-RTOS2 + User 分层业务代码` 的结构。
- 代码依赖方向统一为：`task -> module -> component/device -> bsp -> HAL`。允许上层组合下层能力，禁止下层反向依赖上层任务或具体业务。
- `Src`、`Inc` 负责 CubeMX 生成的启动、外设初始化和中断入口，不承载机器人业务逻辑。
- `User/bsp` 负责板级外设封装，将 HAL 句柄、引脚、DMA 和中断回调转换为稳定的板级接口，不解析具体设备协议。
- `User/device` 负责具体设备驱动和通信协议，例如电机、遥控器、IMU、裁判系统和超级电容，不实现底盘、云台等整机控制策略。
- `User/component` 负责与具体机器人型号无关的通用算法和组件，例如 PID、滤波、姿态解算、限幅、运动学混控、CRC 和 CLI。
- `User/module` 作为目标业务模块层，负责底盘、云台、射击、功率管理和配置等机器人功能。当前项目尚未创建该目录；需要引入时必须遵守 `AGENTS.md` 的源码修改授权范围。
- `User/task` 负责任务创建、周期调度、消息路由和模块编排，不重复实现 BSP、设备协议或通用控制算法。

### 启动与运行时组织

- `main()` 只完成 HAL、时钟、外设、RTOS 内核初始化并启动调度器，不在其中加入业务循环。
- `MX_FREERTOS_Init()` 只创建 CubeMX 管理的 RTOS 对象和业务初始化任务 `Task_Init`，不得逐步演变为业务总入口。
- `Task_Init()` 是业务系统装配入口，集中创建业务任务和消息队列；创建期间可锁定调度器，全部对象创建完成后解锁并终止自身。
- `Task_Runtime_t task_runtime` 集中保存任务句柄、消息队列、系统状态、运行配置和调试统计，避免在各任务文件中散落重复的全局句柄。
- 每个周期任务必须集中定义运行频率、初始化延时、优先级和栈大小，并优先使用 `osDelayUntil()` 保持稳定周期。

### 任务通信与数据流

- 任务之间优先使用消息队列或线程标志通信；中断只采集数据、更新必要状态或通知任务，不在中断中执行控制算法和复杂协议处理。
- 控制链按“输入采集 -> 命令解析 -> 功能模块控制 -> 总线统一输出”组织，设备原始协议必须先转换为系统统一数据结构，再交给控制任务。
- CAN、UART 等总线收发应由对应通信任务统一汇聚和分发，底盘、云台、射击等控制任务不直接操作 HAL 外设。
- `osMessageQueueReset()` 后再 `osMessageQueuePut()` 只适用于“始终只关心最新状态”的邮箱语义；按顺序处理、不可丢失的事件禁止使用该模式。
- 消息队列、任务句柄和回调对象必须先创建并检查返回值，再允许对应任务运行；`Task_Runtime_t` 中声明的每个运行时对象都必须有明确的创建位置。

### 并发与持久化约束

- `osKernelLock()` 只阻止任务调度，不会屏蔽硬件中断；不得用它代替中断临界区，也不得在注释中描述为“防止中断”。
- 被中断和任务共同访问的状态必须根据访问方式使用 `volatile`、原子操作、临界区或消息通知，并明确数据所有权。
- 临界区只保护必须保持一致的最小代码段，禁止在其中执行阻塞等待、Flash 擦写、日志输出或耗时控制计算。
- Flash 配置优先保存名称、标识和可校准数值；运行时函数指针、对象指针和 RTOS 句柄不得作为可跨固件版本复用的持久化数据。

### 新功能归属

- 新增板级接口放入 `User/bsp`，新增设备或协议放入 `User/device`，新增通用算法放入 `User/component`。
- 新增底盘、云台、射击等完整业务能力放入 `User/module`；`User/task` 只负责调用模块、交换消息和控制执行周期。
- 新增功能前先确定所属层和输入输出数据结构，再建立任务与消息流；禁止先把实现堆入 `main.c`、`freertos.c` 或单个任务文件后再拆分。

## 代码注释

- 新增或修改结构体时，必须在结构体定义上方使用简体中文集中说明结构体用途，并逐项说明每个字段的含义、单位、范围或状态条件；不要使用分散的行尾注释。
- 一个函数或任务包含多个处理阶段时，必须在初始化、数据获取、校验、计算、发布和故障处理等实际存在的阶段前写简体中文目的注释；注释说明阶段目的，不逐行翻译代码。
- `while` 循环内调用的每个函数都必须在调用前使用简体中文注释说明本次调用的目的；相邻调用属于同一阶段时可以共用一条能够覆盖共同目的的阶段注释。
- 禁止给简单赋值、显而易见的条件判断或函数名已经完整表达用途的语句添加逐行翻译式注释。
- 普通函数的返回值不需要使用时直接调用函数，不在函数调用前添加不必要的 `(void)`；未使用的函数参数可以使用 `(void)参数名` 消除编译警告。

## 代码排版

- 项目默认单行宽度为 120 列。简单变量声明、赋值、函数调用、`return` 和短条件判断在不超过 120 列时必须写在一行内。
- 禁止在赋值运算符 `=` 后对可单行表达的语句换行，也禁止拆分能在单行容纳的函数名和参数列表。
- 只有整条语句超过 120 列，或表达式包含多个需要分组展示的独立逻辑条件时才允许换行。
- 花括号换行规则优先于单行规则。
- 不得用格式化工具制造无意义换行；格式化后必须人工复核，并将 120 列内可以清晰表达的声明、赋值、调用、`return` 和短条件恢复为单行。

## 花括号格式

- 所有成对花括号均独占一行，左花括号与对应的右花括号上下同列对齐。
- 函数、条件分支、循环、`switch`、结构体、联合体、枚举和多行初始化均使用上述花括号格式。
- 禁止使用 `} else {` 和 `} else if (...) {`，统一使用 Allman 风格换行。

```c
if (condition)
{
  DoSomething();
}
```

## 编码后强制检查

- 每完成一个源码文件或一个独立实现改动点，必须立即重新读取根目录 `AGENTS.md` 和 `agent.md` 并检查本次改动，不得拖到整个任务结束后处理。
- 自动扫描同行左花括号、`} else {`/`} else if`、缺失的控制块花括号、超过 120 列、行尾空白和可单行却被机械拆分的语句。
- 人工核对处理阶段中文目的注释、逐行翻译式无效注释、Doxygen 参数与返回值、结构体字段集中说明和分层依赖。
- 检查未通过时必须先修正并重新执行检查；每个实施项还必须运行对应编译、静态检查和测试，保留验证证据后才能标记完成。
