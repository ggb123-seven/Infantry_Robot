# 协作规则

- 源码修改范围默认严格限制在 `User/task` 目录；未经用户明确同意，不得修改 `User/bsp`、`User/device`、`applications`、`Src`、`Inc`、`Drivers`、`Middlewares` 或其他源码目录。
- 查看、分析和对比非 `User/task` 源码不需要额外授权；如果任务依赖修改 BSP、设备驱动、生成代码或其他底层源码，必须先暂停并向用户说明依赖关系，取得明确同意后才能修改。
- 文档、日志、构建配置和协作规则文件按用户当前任务授权处理，不受 `User/task` 源码目录限制；不得借此修改或生成其他目录的源码文件。
- 任何 CubeMX 配置变更必须先取得用户明确同意；未经同意不得修改 `.ioc` 文件中的芯片、引脚、时钟、外设、DMA、中断、FreeRTOS 或生成器配置。
- 查看、分析和对比 `.ioc` 不需要额外授权；提出 `.ioc` 修改建议时只提供方案和差异，由用户自行在 CubeMX 中执行，除非用户明确要求我修改并确认范围。
- CMake、Keil 和 VS Code 配置文件按用户当前任务授权处理，但若修改是为了配合尚未获准的 CubeMX 配置，则应先暂停并说明依赖关系。
- 我新增或修改的代码注释必须使用简体中文，并以 UTF-8 编码保存；代码注释中的说明、参数解释和设计备注不得使用英文。
- CubeMX 生成代码、厂商 HAL/CMSIS/FreeRTOS 等自动生成或第三方框架代码中的既有注释不主动改写；只有我实际新增或修改的注释受上一条规则约束。
- Git 提交主题和正文统一使用简体中文；命令、路径、代码标识符、API 名称和确需保留的专有名词可以使用英文。

## 代码框架结构

### 总体架构

- 项目采用 `STM32CubeMX 生成底座 + STM32 HAL + FreeRTOS/CMSIS-RTOS2 + User 分层业务代码` 的结构。
- 代码依赖方向统一为：`task -> module -> component/device -> bsp -> HAL`。允许上层组合下层能力，禁止下层反向依赖上层任务或具体业务。
- `Src`、`Inc` 负责 CubeMX 生成的启动、外设初始化和中断入口，不承载机器人业务逻辑。
- `User/bsp` 负责板级外设封装，将 HAL 句柄、引脚、DMA 和中断回调转换为稳定的板级接口，不解析具体设备协议。
- `User/device` 负责具体设备驱动和通信协议，例如电机、遥控器、IMU、裁判系统和超级电容，不实现底盘、云台等整机控制策略。
- `User/component` 负责与具体机器人型号无关的通用算法和组件，例如 PID、滤波、姿态解算、限幅、运动学混控、CRC 和 CLI。
- `User/module` 作为目标业务模块层，负责底盘、云台、射击、功率管理和配置等机器人功能。当前已创建，现有底盘控制 `chassis` 和故障检测 `fault_detect` 模块；后续新增或修改必须遵守项目源码修改授权范围。
- `User/task` 负责任务创建、周期调度、消息路由和模块编排，不重复实现 BSP、设备协议或通用控制算法。

### 启动与运行时组织

- `main()` 只完成 HAL、时钟、外设、RTOS 内核初始化并启动调度器，不在其中加入业务循环。
- `MX_FREERTOS_Init()` 只创建 CubeMX 管理的 RTOS 对象和业务初始化任务 `Task_Init`，不得逐步演变为业务总入口。
- `Task_Init()` 是业务系统装配入口，集中创建业务任务和消息队列；创建期间可锁定调度器，全部对象创建完成后解锁并终止自身。
- `Task_Runtime_t task_runtime` 集中保存任务句柄、消息队列、系统状态、运行配置和调试统计，避免在各任务文件中散落重复的全局句柄。
- 每个周期任务必须集中定义运行频率、初始化延时、优先级和栈大小，并优先使用 `osDelayUntil()` 保持稳定周期。

### 任务通信与数据流

- 任务之间优先使用消息队列或线程标志通信；中断只采集数据、更新必要状态或通知任务，不在中断中执行控制算法和复杂协议处理。
- 控制链按“输入采集 -> 命令解析 -> 功能模块控制 -> 总线统一输出”组织，设备原始协议必须先转换为系统统一数据结构，再交给控制任务。
- CAN、UART 等总线收发应由对应通信任务统一汇聚和分发，底盘、云台、射击等控制任务不直接操作 HAL 外设。
- `osMessageQueueReset()` 后再 `osMessageQueuePut()` 只适用于“始终只关心最新状态”的邮箱语义；按顺序处理、不可丢失的事件禁止使用该模式。
- 消息队列、任务句柄和回调对象必须先创建并检查返回值，再允许对应任务运行；`Task_Runtime_t` 中声明的每个运行时对象都必须有明确的创建位置。

### 并发与持久化约束

- `osKernelLock()` 只阻止任务调度，不会屏蔽硬件中断；不得用它代替中断临界区，也不得在注释中描述为“防止中断”。
- 被中断和任务共同访问的状态必须根据访问方式使用 `volatile`、原子操作、临界区或消息通知，并明确数据所有权。
- 临界区只保护必须保持一致的最小代码段，禁止在其中执行阻塞等待、Flash 擦写、日志输出或耗时控制计算。
- Flash 配置优先保存名称、标识和可校准数值；运行时函数指针、对象指针和 RTOS 句柄不得作为可跨固件版本复用的持久化数据。

### 新功能归属

- 新增板级接口放入 `User/bsp`，新增设备或协议放入 `User/device`，新增通用算法放入 `User/component`。
- 新增底盘、云台、射击等完整业务能力放入 `User/module`；`User/task` 只负责调用模块、交换消息和控制执行周期。
- 新增功能前先确定所属层和输入输出数据结构，再建立任务与消息流；禁止先把实现堆入 `main.c`、`freertos.c` 或单个任务文件后再拆分。

## 代码排版

- 项目默认单行宽度为 120 列。简单变量声明、赋值、函数调用、`return` 和短条件判断在不超过 120 列时必须写在一行内，不得为了对齐或套用格式化模板主动拆行。
- 禁止把本可单行表达的语句在赋值运算符 `=` 后换行，也禁止把能在单行容纳的函数名与参数列表拆成多行。
- 只有整条语句超过 120 列，或表达式包含多个需要分组展示的独立逻辑条件时才允许换行；换行后按参数或逻辑分组缩进，不做无语义的机械断行。
- 花括号换行规则优先于单行规则；不得为了把控制块压成一行而省略规定的花括号换行。
- 不得直接接受格式化工具产生的机械换行；使用格式化工具后必须人工复核，并把 120 列内可以清晰表达的声明、赋值、调用、`return` 和短条件恢复为单行。

## 花括号格式

- 代码中的左花括号 `{` 必须另起一行，右花括号 `}` 必须独占一行，并与对应语句的起始位置对齐。
- 函数、条件分支、循环、`switch`、结构体、联合体、枚举和多行初始化统一使用上述格式。
- `} else {`、`} else if (...) {` 等同一行写法禁止使用，必须将右花括号、`else` 和左花括号分别按 Allman 风格换行。

## 注释格式

- 我新增或修改的代码注释末尾不使用句号（`。` 或 `.`）
- 函数说明使用 Doxygen 风格块注释，统一使用简体中文：
  - `@brief`：说明函数用途。
  - `@param`：逐项说明每个参数，包含参数含义、单位或取值约束；输入参数可标注 `[in]`，输出参数可标注 `[out]`，输入输出参数可标注 `[in,out]`。
  - `@return`：说明返回值含义、状态或错误条件；无返回值函数使用 `@return` 或 `@retval None`，与项目现有风格保持一致。
- Doxygen 函数注释建议按以下顺序组织：`@brief`、空行、`@param` 列表、`@return`/`@retval`，最后结束注释块。
- 函数体内的行注释使用 `//`，每条只说明一个关键动作、协议约束、边界条件或故障处理；注释写在相关代码之前，不写无信息量的逐行翻译。
- 一个函数或任务包含多个处理阶段时，必须在初始化、数据获取、校验、计算、发布和故障处理等实际存在的阶段前写简体中文目的注释，说明该阶段为何执行；不得给每条赋值或显而易见的函数调用添加逐行翻译式注释。
- `while` 循环内调用的每个函数都必须在调用前写简体中文目的注释；相邻调用属于同一处理阶段时，可以使用一条阶段注释统一说明，但注释必须覆盖这些调用的共同目的。
- 多个参数、引脚、协议字段、阈值或配置项的集中说明，写在对应参数表、`#define XXX_PARAM_LIST` 或结构体定义上方；不要把说明分散成行尾注释。
- 页面合并或参数迁移后，必须同步更新参数表上方的集中说明，保证注释与实际字段一致。
- 协议位域、CAN ID、滤波器、DMA、时钟等魔数必须在代码前用中文说明其含义、单位、范围和来源；需要时标注 `@datasheet`、`@netlist` 或配置来源。
- 注释文件统一保存为 UTF-8；我新增或修改的注释不得使用英文，自动生成代码和第三方代码的既有注释除外。

## 编码后强制检查

- 每完成一个源码文件或一个独立实现改动点，必须立即重新读取根目录 `AGENTS.md` 和 `agent.md`，再检查本次改动；不得等到整个任务完成后统一检查。
- 必须扫描并修正：同行左花括号、`} else {`/`} else if`、应使用却缺失的控制块花括号、超过 120 列、行尾空白，以及 120 列内可以单行却被机械拆分的语句。
- 必须人工检查：每个处理阶段是否有简体中文目的注释、是否存在逐行翻译式无效注释、Doxygen 参数和返回值是否完整、结构体字段是否集中说明。
- 必须检查分层依赖、返回值处理、RTOS/HAL 对象创建结果、ISR 共享状态和默认输出安全性；发现不符合项时先修正并重新检查，再继续下一个改动点。
- 每个实施清单项完成后运行与风险相匹配的编译、静态检查和测试；未提供检查结果与验证证据，不得宣称该项完成。
